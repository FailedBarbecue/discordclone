import React from 'react';

import { Container, HashtagIcon, Title, Separator, Description } from './styles'

const ServerName: React.FC = () => {
    return (
        <Container>
            <HashtagIcon />

            <Title>poggers</Title>
            <Separator />
            <Description>Comunidade do TCC</Description>
        </Container>
    );
}

export default ServerName;