import React from 'react';

import { Container, Title, ExpandIcon } from './styles'

const ServerInfo: React.FC = () => {
    return (
        <Container>
            <Title>DINOSOWER !</Title>

            <ExpandIcon />
        </Container>
    );
}

export default ServerInfo;